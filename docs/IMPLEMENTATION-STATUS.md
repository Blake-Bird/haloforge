# Implementation status

## Scope

All requirements in OBJECTIVE.md and REQUIREMENTS.md remain in scope. Work is underway; the application is not release-approved.

Current local verification: 185 tests pass, including clean-storage Streamlit startup, prediction-first, and hosted-mode fail-closed UI tests; the maintained formatter and static-correctness checks also pass. The exact pinned AxiCLASS source was also built in an isolated Python 3.11 environment: HaloForge's real CLASS smoke solve and a reduced EDE AxiCLASS solve completed. Historical test counts below are retained as a development record, not as the current suite total.

## Verified progress

- Reoriented first use around an Explore experiment: a prediction-first EDE prompt stages one meaningful change before advanced controls, then explains the causal chain and offers conceptual zoom from intuition through implementation. Added structured parameter-delta explanations with direct equations, limitations, and falsification checks; this is curated logic for supported changes, not a general-purpose scientific conclusion engine.
- Added a local experiment notebook to each run: question, hypothesis, prediction, conclusion, caveats, citations, annotations, parent-run lineage, parameter differences, and reproducibility hash. Notebook data and a caveat-carrying share card are included in each bundle; this is not collaboration, authentication, or a hosted sharing service.
- Added explicit local display choices for dark, light, high-contrast, and reduced-motion use, removed the Google-font dependency, and added a table/CSV transcript to each rendered Plotly chart. Browser inspection covered the Explore and display-control entry points; comprehensive accessibility review and device/screen-reader testing remain required.
- Extended run exports with documented CSV/Parquet tables, notebook and share-card artifacts, a Python AxiCLASS recreation starter, and Julia/R/Mathematica table-loading starters. Independently verified cross-platform recreation remains incomplete.
- Added a scientific-assurance panel that separates successful computation, sampled-range sensitivity, physical appropriateness, fit calibration, cosmology support, and publication suitability. It intentionally refuses to upgrade a curve to publication-ready without independent validation. Current suite: 95 tests passing.
- Added local, reusable teaching modules for primordial tilt, numerical coverage, and EDE structure formation. Each has learning objectives, prerequisites, an estimated completion time, a student handout, instructor guide, falsifiable checkpoint, caution, and Jupyter notebook template. This is prepared teaching material, not a classroom platform, assignment system, grading service, or evidence of learning outcomes. Current suite: 101 tests passing.
- Added non-destructive figure recipes for interactive, paper-draft, lecture, student-explanation, and share-card presentations. Recipes preserve plotted values and scales, and paper-draft output explicitly remains subject to caption, citation, calibration, external-validation, and author review. Current suite: 103 tests passing.
- Rebuilt primary navigation around Explore, Compare, and Research. Explore hides advanced controls; Compare contains controlled comparison and sensitivity work; Research groups the detailed laboratory tools. This is an interface consolidation, not proof that every advanced workflow is complete. Current suite: 103 tests passing.
- Added a versioned, visible known-limitations registry covering external-reference gaps, EDE HMF calibration, finite k range, illustrative structure fields, hosted collaboration, and unverified accessibility. Each entry names its impact and mitigation. Current suite: 104 tests passing.
- Upgraded notebook annotations from free text to optional named chart regions with saved x-coordinate bounds. Matching active-run plots render those regions and their notes; malformed or inverted regions are rejected. Current suite: 105 tests passing.
- Expanded Explore into a curated local experiment library for early expansion, small-scale primordial tilt, and numerical k-range coverage. Each gives a question, expected evidence path, prediction prompt, baseline-respecting parameter change, and caveat. Current suite: 105 tests passing.
- Added an uncertainty and validity inventory that exposes numerical integration, finite k range, grid resolution, interpolation, solver settings, HMF fit calibration, cosmology support, theory uncertainty, user sensitivity, and emulator state separately. Missing quantification stays visibly unquantified. Current suite: 106 tests passing.
- Added a Convergence Lab that groups sampled-range coverage, low/high-k endpoint removal checks, saved adaptive-integration benchmark results, and suggested one-parameter numerical sweeps. It does not claim complete solver or physical convergence. Current suite: 106 tests passing.
- Updated the paper-draft figure recipe so Plotly’s client-side image export uses SVG rather than PNG. Font embedding and independent publication-figure review remain incomplete. Current suite: 106 tests passing.
- Added a transparent experiment-design assistant with interpretable EDE, tilt, and numerical-coverage plans. It shows the parameter diff, fixed assumptions, expected evidence path, and caveat, then stages a local candidate only on explicit user action. This is not active learning or an automated conclusion engine. Current suite: 108 tests passing.
- Strengthened isolated CLASS/AxiCLASS execution with a validated timeout, a bounded retry only for infrastructure-like worker failures, and explicit no-retry outcomes for timeout or CLASS-reported invalid settings. Successful results record the applied execution policy and attempt count. Current suite: 110 tests passing.
- Added a versioned, machine-readable scientific-validity artifact to every saved run and export. It preserves the six separate assurance claims, named uncertainty evidence, and HMF-contract context without collapsing them to a confidence score or automatically approving publication use. Current suite: 112 tests passing.
- Added saved-run integrity verification. New runs checksum their NPZ payload and independently recompute the declared calculation identity on load; a mismatch is visibly flagged and cannot hydrate the active analysis session. Export bundles now carry `scientific_validity.json` and `integrity.json` alongside their manifest. Legacy runs remain readable but explicitly unverified. Current suite: 113 tests passing.
- Hardened the local AxiCLASS result cache with a versioned envelope, requested-settings binding, content checksum, and basic power-spectrum invariant checks. Corrupt, stale, or mismatched cache artifacts are deleted and recomputed rather than used. Current suite: 115 tests passing.
- Added a saved Performance Lab probe for the local σ(M) and eligible HMF core. It records median rebuild time and Python allocation peak against explicit budgets, exports the result with each bundle, and states which end-to-end budgets (browser, solver startup/RSS, representative export) remain unmeasured rather than fabricating them. Current suite: 117 tests passing.
- Added explicit, non-destructive saved-run schema migrations. Legacy documents gain a versioned in-memory migration record that preserves original fields and becomes durable only on a later atomic save; unknown newer schemas fail closed. Export bundles include `migration.json` so recovery history travels with the calculation. Current suite: 120 tests passing.
- Added local, non-graded scientific-skepticism exercises that ask learners to identify an unjustified conclusion in smooth-but-uncalibrated HMF output, k-range coverage changes, and illustrative linear fields. Each explains why the claim overreaches and names the next evidence step; no student answers are stored or collected. Current suite: 121 tests passing.
- Added conservative automatic graph insights: the observed top-hat contribution peak for a selected mass, sampled HMF fit-calibration transitions, and the largest finite baseline deviation in ratio/residual comparisons. These markers do not interpolate, smooth, or upgrade scientific validity; they make the stated evidence easier to inspect. Current suite: 124 tests passing.
- Extended conservative automatic graph insight cues with sampled local turning points on multiplicity relations and exact-or-bracketed reference crossings in baseline comparisons. A crossing band names only the two stored samples on opposite sides of the reference; it does not infer a continuous crossing coordinate. These cues expose structure in the plotted data without manufacturing precision, convergence evidence, or a calibration claim.
- Added a selected-k-range-to-mass contribution mapper. Users choose a sampled Fourier interval and receive a ranked table and chart of the stored halo masses whose σ² receives the largest fraction from it. The calculation states its sampled-grid, non-causal, non-convergence scope limit. Current suite: 125 tests passing.
- Added a one-page PDF run report to every reproducibility bundle, covering submitted parameters, provenance, saved validity claims, notebook context, caveats, and derived values. It is generated atomically, included in the manifest and ZIP, checked with pypdf, and visually rendered for QA. It is a report artifact, not a replacement for publication-ready PDF figure export. Current suite: 125 tests passing.
- Added vector PDF figure exports to every eligible bundle: linear matter power, mass variance, and analytic HMF references. Each includes units, a concise evidence-limiting caption, and is hashed into the manifest and ZIP; rendered sample pages were visually checked. These figures remain subject to citation, calibration, and author review before publication. Current suite: 125 tests passing.
- Added `citation_metadata.json` to every bundle. It maps each exported figure to solver provenance, numerical-method context, HMF ingredients/fit contracts, and user-supplied notebook citations, while explicitly distinguishing this metadata from independent external validation. Figure captions now point directly to the file. Current suite: 126 tests passing.
- Made the scientific Parquet exports self-describing. `power_spectrum.parquet` and `sigma.parquet` now carry versioned field units and descriptions, the run/reproducibility identity, and explicit links to provenance, validity, integrity, and citation records in the same bundle; ordinary Pandas reads remain supported. The metadata states that it is context, not evidence of calibration or publication suitability. Current suite: 126 tests passing.
- Made follow-up evidence explicit in the local research notebook. A run can now link named convergence checks, fit comparisons, or other saved experiments without changing their branch parents; links travel in `notebook.json`, resolve by stable run ID through renames, and missing imported IDs stay visibly reported rather than being silently discarded. Current suite: 127 tests passing.
- Completed the static figure-format portion of research bundles: each eligible matter-power, mass-variance, and analytic-HMF figure now exports as matching PDF, SVG, and high-resolution PNG from one captioned source figure. Citation metadata names all three formats, checksums and ZIP contents include them, and batch rendering keeps the complete suite practical. A generated PDF page was rendered and visually inspected for legibility and clipping. Current suite: 127 tests passing.
- Added a self-describing `hmf.parquet` alongside the analytic HMF CSV. It records units, analytic-top-hat mass convention, source models, finite-grid cumulative semantics, reproducibility identity, and links to provenance/validity/citation records. Its schema directly warns that it is not a universal empirical calibration. Current suite: 127 tests passing.
- Added deliberate grayscale-safe static figure variants for every eligible bundled plot. They retain the identical data, captions, validity limits, citation links, checksums, and PDF/SVG/PNG formats while using contrast and solid/dashed line patterns to preserve series identity in print. A rendered monochrome HMF page was visually checked; an initially overlong caption was corrected to a readable second line. Current suite: 127 tests passing.
- Expanded conceptual zoom from four hard-coded explanations to eight core ideas named by the product brief: Aₛ, nₛ, transfer function, σ(M), σ₈, top-hat filtering, EDE, and halo mass definition. Each now has distinct intuition, course, research, and implementation layers that retain equations, explicit calibration/numerical limits, and concrete code/provenance context. Current suite: 129 tests passing.
- Added bounded “Compare at a physical point” analysis to Compare Lab. Users can select an observable, candidate, compatible window/fit when relevant, and a coordinate inside the shared stored domain to see candidate and baseline values with units, exact ratio, percent difference, sampling/interpolation method, and named uncertainty context. Out-of-domain and zero-baseline ratios fail visibly rather than extrapolating or fabricating a result. Current suite: 131 tests passing.
- Added one-click local “teach this tomorrow” module bundles. Each ZIP contains a student handout, assignment brief, instructor guide, solution guide, locally loading notebook template, accessibility notes, README, and checksummed manifest. The bundle repeatedly states its local/no-surveillance boundary: it does not create accounts, collect answers, analyze students, or export grades. Current suite: 132 tests passing.
- Strengthened reduced-motion behavior: the existing in-app pause toggle remains available, and the stylesheet now automatically honors the operating system’s `prefers-reduced-motion` setting before users discover the control. Motion continues to have static equivalents; independent assistive-technology and device review remains open. Current suite: 133 tests passing.
- Added a conservative saved-evidence counterfactual in Sensitivity Explorer. Given a target observable change and direction, it identifies the smallest qualifying one-parameter perturbation already present in controlled saved runs; when none qualifies, it explicitly refuses to interpolate, extrapolate, emulate, or invent an unrun threshold. Current suite: 135 tests passing.
- Improved publication-figure scientific typography: static PDF/SVG/PNG axes and hover labels now use readable units such as Mpc⁻¹, Mpc³, h⁻¹ M☉, h³ Mpc⁻³, and σ(M), replacing code-style exponent strings. A rendered mass-variance PDF was visually inspected for glyph coverage and clipping. Current suite: 135 tests passing.
- Added `data_dictionary.json` to every reproducibility bundle. It is derived from the same versioned table metadata as Parquet and lists each included table's CSV/Parquet files, every column name, unit, definition, provenance/validity/integrity/citation links, plus HMF mass-definition and calibration context where applicable. Current suite: 135 tests passing.
- Strengthened selected-mass synchronization: the shared inspection mass is now visibly marked in mass-radius, σ(M), and HMF plots as well as driving the variance-contribution view and detailed inspector. The marker conveys selection, not a claimed feature, convergence result, or calibrated HMF conclusion. Current suite: 135 tests passing.
- Performed a live local browser inspection of the first-use Explore screen after the recent onboarding/concept/accessibility work. The prediction-first EDE prompt, advanced-control-free entry state, eight-concept zoom selector with all four layers, and reduced-motion control rendered and exposed accessible controls correctly; toggling reduced motion changed state. This is local UI evidence only, not independent accessibility or device testing.
- Added the first architecture decision record, [ADR 0001](adr/0001-run-export-integrity-and-provenance.md), for durable run/export integrity and provenance. It documents atomic persistence, fail-closed payload checks, self-describing table metadata, the data-dictionary decision, and the hard boundary between reproducibility records and scientific validity. Current suite: 135 tests passing.
- Added [ADR 0002](adr/0002-halo-mass-definition-and-calibration-contracts.md) for HMF mass-definition and calibration contracts. It records the fail-closed convention policy, visible-but-dotted extrapolation semantics, analytic-versus-empirical distinction, and the separate unresolved question of cosmology support. Current suite: 135 tests passing.
- Hardened local workspace import preflight and added deterministic malformed-archive fuzz coverage. Windows-style separators and control characters are rejected alongside traversal, absolute, and unknown-root paths; every fuzz case confirms that a valid sibling file is not written when any member is unsafe. A ZIP NUL-name fixture was intentionally excluded because Python's ZIP writer truncates it before import preflight can observe it. Current suite: 141 tests passing.
- Extracted the illustrative linear Gaussian structure-field generator from Streamlit into a pure scientific-core module. It now has direct deterministic-seed, seed-sensitivity, finite-output, and invalid-grid tests; the Streamlit layer only caches and displays it. The 3D field is mean-subtracted before slicing, while an individual displayed slice is correctly not asserted to have zero mean. Current suite: 143 tests passing.
- Hardened external/programmatic parameter validation. Missing, non-numeric, non-finite, non-integral grid-count, and malformed-redshift values now produce aggregated actionable validation messages instead of raw conversion failures, while valid defaults remain accepted. Current suite: 145 tests passing.
- Added a prepared local lecture mode to every teaching module. It presents a six-step prediction → causal chain → evidence → skepticism narrative in large, high-contrast projector typography; the focused native slide selector supports left/right-arrow navigation, and speaker notes plus portable Markdown/JSON lecture materials are included in the checksummed teaching bundle. This is a local presentation aid, not a classroom, response collection, analytics, or grading system. Current suite: 148 tests passing.
- Strengthened the canonical Explore reveal. A guided experiment now saves a matched Planck-like baseline before the one-change candidate, derives the displayed z=0 10¹⁴ h⁻¹ M☉ abundance difference only from those bounded stored curves, preserves the baseline as the candidate's notebook parent, and directly opens the chosen Understand, Compare, or Investigate route. The headline is explicitly not an HMF calibration, theory-uncertainty, or publication-suitability claim. Current suite: 148 tests passing.
- Made the Explore prediction genuinely deliberate: no outcome is selected by default, and staging is disabled until a learner selects one—including “I am not sure yet.” The chosen outcome is then saved as the prediction/hypothesis alongside the candidate; a stale or unselected value is rejected by the scientific-core helper instead of being silently converted into a claim. Local UI inspection confirmed the initially disabled state and accessible unselected controls.
- Turned the guided result into a deliberately staged causal reveal: early condition, linear matter power, smoothed variance, and halo abundance. It shows one focused stage at a time through a keyboard-operable selector; each plotted stage retains the standard caption, caution, and accessible transcript rather than making animation necessary for comprehension. Current suite: 148 tests passing.
- Added a versioned canonical validation-case registry and staging flow for ΛCDM, EDE, curvature, high-redshift, low-mass, high-mass, and an intentional Tinker calibration-boundary scenario. The Benchmark Lab visibly distinguishes a run whose settings cover a case from an externally benchmarked result; no frozen CLASS/CAMB/Colossus/hmf expected-output table is claimed or bundled. Current suite: 152 tests passing.
- Made validation-case staging explicit-state safe: requested overrides are applied at the beginning of the next Streamlit render, before sidebar controls are created, so stale widget values cannot silently replace a selected case's settings. Current suite: 152 tests passing.
- Added portable local lab-section artifacts for instructors: a locked baseline, explicitly permitted parameter ranges, an evidence-first question sequence, estimated time, and separate student/instructor ZIPs. The student ZIP omits solution material and the instructor companion carries it, but both the UI and artifacts state that this is an operational separation—not hidden solutions, roles, accounts, analytics, or authenticated classroom infrastructure. Live local UI inspection confirmed the section controls and boundary notice; full suite: 152 tests passing.
- Added scientific cursor context to every interactive Plotly trace. Hovers now retain stored x/y values and units while explaining what the selected point represents and what it cannot establish; unknown figures receive an explicit stored-sample/assumptions fallback. The existing captions, caution text, and accessible tables remain the non-hover equivalents. Current suite: 156 tests passing.
- Added explicit brush-to-notebook interaction for charts. Box/lasso selections yield only finite, non-degenerate x-ranges; a user must provide a note and press save before a region becomes a saved run annotation and export-bundle update. The selection never changes calculation data or silently creates a convergence claim. Current suite: 156 tests passing.
- Added a named error taxonomy for solver timeout/busy/unavailable, invalid configurations, unsafe imports, stored-data issues, and unknown interruptions. Major calculation, guided-run, benchmark, and workspace-import failures now give a remedy, preserve the prior-work context, and expose exact detail only in a user-opened technical expander. Guided runs restore their staged candidate even after an unexpected interruption. Current suite: 159 tests passing.
- Added privacy-preserving opt-in observability. Disabled by default, it stores at most 200 local calculation/benchmark/failure categories and optional durations in the app-data vault; it never records cosmological parameters, run IDs, notes, IP addresses, or network telemetry. Users can see the aggregate count and delete the local diagnostic history from the accessibility/settings popover. Current suite: 162 tests passing.
- Updated the public-facing unreleased changelog with the current guided exploration, teaching, graph, validation, export, reliability, and privacy work, and linked it from the README alongside the implementation status, limitations registry, and release policy. The entry explicitly does not claim external scientific, accessibility, classroom, security, or deployment validation.
- Added a stored-data cosmic fingerprint for matched share cards and Notebook. It summarizes tracked parameter changes plus σ₈, pivot-scale linear power, and selected-mass linear variance only when they exist in both saved runs; it explicitly excludes literal-universe, observation, uncertainty, and empirical-HMF-calibration claims. Current suite: 164 tests passing.
- Added a hash-linked, append-only local mutation trail to each saved run. It records creation, metadata field changes, renames, baseline selection, and forks without storing changed parameter values or notebook text; the Notebook displays verification state and bundles include `audit_trail.json`. It detects changes or reordering of retained records but cannot prevent filesystem edits, and is explicitly not an authenticated, multi-user, or server-secured research-workspace audit history. Current suite: 172 tests passing.
- Made attached artifacts explicit in the local research notebook. Every generated export associated with a selected saved run is listed with its filename, local presence, and byte size, and the user can download a compact attachment manifest. Missing files remain clearly absent rather than being silently represented as present. This is a local run-to-export relationship, not a shared attachment service, immutable shared snapshot, or external archival system.
- Extended prepared, local-only teaching modules across introductory cosmology, computational physics, statistics, structure formation, and numerical methods. The new rare-tail and numerical-convergence modules include prediction prompts, bounded evidence tasks, instructor moves, skepticism cautions, lecture arcs, accessible handouts, and reproducible notebook templates through the same teaching-bundle contract. The guided calculation state is also closed after a successful run so it cannot silently generate duplicate baseline/candidate pairs.

- Corrected Tinker08 overdensity dependence: all four coefficients now use tabulated values, with bounded spline interpolation. Reference-node and independent-spline checks cover Δ=200, 800, 3200 and an intermediate value. Invalid overdensities are rejected, with UI feedback for unsupported plots and submitted settings.
- Differential HMF now validates input arrays and physical scales and preserves zero underflow instead of adding a positive floor. Invalid sigma is rejected by the multiplicity functions.
- Current suite: 84 tests passing. Tinker calibration enforcement, halo mass-definition selection, EDE background quantities, and independent solver benchmarks remain incomplete.
- Added CLASS/AxiCLASS-derived Ωm(z) to the isolated worker result, cache, run payload, restored sessions, and comparison pipeline. Watson SO uses it; EDE Watson SO fails closed if the solver did not provide a valid background. Current suite: 71 tests passing. This records the correct expansion history source, but Watson's calibration for EDE remains unproven.
- Added a central HMF contract registry covering analytic top-hat, FOF b=0.2, and SO-mean definitions, fit redshift/ln(σ⁻¹)/Δ ranges, citations, and pointwise calibration masks. Incompatible definitions raise before evaluation. HMF plots render supported points solid and numerical extrapolation dotted. Stored Press–Schechter and Sheth–Tormen references are explicitly analytic even when the active run uses an empirical halo definition. Current suite: 78 tests passing.
- Replaced raw kR diagnostics with a saved, user-visible numerical report: sampled-range heuristics plus controlled reintegration after removing high- and low-k endpoint ranges. The report states its scope limit and does not label this internal sensitivity check as solver or physical convergence. Current suite: 81 tests passing.
- Every newly created run now receives a versioned provenance record and deterministic reproducibility hash based on parameters, exact solver settings, application revision, AxiCLASS revision (when available), package versions, and requirements fingerprint. Exports include provenance.json plus a manifest of file hashes and sizes; the summary report carries the same identifiers. This is an audit trail, not a substitute for independently reproducing the calculation. Current suite: 84 tests passing.
- Local storage now defaults to the operating-system app-data directory rather than the cloned repository, and Docker Compose uses a named volume. The app refuses unauthenticated hosted filesystem persistence before accessing a run, draft, cache, or export; browser-private or authenticated multi-user storage is still not implemented. Current suite: 90 tests passing.
- Local full-workspace imports now preflight every archive member, reject traversal/duplicate/oversize archives, require explicit acknowledgement before replacing an existing file, and verify a workspace checksum manifest when present. Legacy archives are shown as unverified. Container launch remains unverified because Docker Desktop was not running on this machine; `docker compose config` does validate.
- Added a deterministic GitHub Actions workflow (tests, dependency audit, image build), a citable software record, security and contribution guidance, and a release-evidence policy. No open-source license has been assumed; the rights holder must select one before a public code release.

- Extracted baseline curve comparison into the scientific core. Ratios no longer extrapolate beyond the baseline domain; zero denominators remain undefined. The comparison caption explains gaps and differing cumulative integration bounds.
- Replaced repeated cumulative HMF integration with a reverse cumulative trapezoidal sum, retaining the exact zero endpoint and validating mass/abundance inputs. This computes a finite upper-bound integral, explicitly documented rather than implying an integral to infinity.
- Verified 43 tests pass, including power-law interpolation, disjoint baseline edges, zero denominators, identical-curve residuals, invalid grids, and comparison of cumulative integration with independent per-threshold SciPy integrals. This is partial numerical evidence, not full scientific validation.

- Preserved the complete objective and extracted 384 listed requirements with explicit open status.
- Added Git and Docker exclusion rules for personal data and development artifacts.
- Replaced broad Docker source copying with an explicit application directory allowlist.
- Configured the runtime image to use an unprivileged application user. Container build and mounted-volume write permissions still require verification.

Existing data is preserved on disk. Ignore rules do not remove files from an existing Git index; this supplied folder has no Git metadata. No public release has been made.

## Next work

1. Implement storage ownership across browser, local application, and authenticated collaborative operation; replace global server vault semantics.
2. Implement scientific contracts and verified fit/mass-definition compatibility, then numerical reference and convergence coverage.
3. Build the new interface, learning content, experiment notebook, exports, classroom flows, and research tools against those contracts.
4. Launch locally, inspect real workflows, and gather requirement-specific evidence. A successful local launch will not establish external review, classroom adoption, or learning improvement.

## External evidence still required

Independent scientific, accessibility, design, and classroom reviews; real participant learning measurements; advisory participation; public academic usage. Do not invent endorsements, test participants, case studies, or adoption statistics.
